{
  "name": "telegram-mini-app",
  "version": "1.0.0",
  "description": "Point system with Telegram backup",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^1.6.5"
  },
  "engines": {
    "node": ">=18"
  }
}